(in-package :bonus)

(defparameter *db-file* nil
  "Path to SQLITE database")

(defun init-db ()
  "Initialize database")

(defun remove-db ()
  "Delete database")

(defun store-result ()
  "Store result in database")

(defun store-results ()
  "Store results in database")

(defun retrieve-results ()
  "Retrieve result from database")

(defun retrieve-results ()
  "Retrieve results from database")
